export const metadata = {
  title: 'Order Placed — Thank You | Secret Hour',
  description: 'Your Secret Hour order has been received. We will be in touch shortly.',
  robots: { index: false, follow: false },
};

export default function ThankYouLayout({ children }) {
  return children;
}
